export interface AboutModel {
  title: string;
  contact: string;
  description: string;
  copyright: string;
}

export interface CartModel {
  title: string;
  empty: string;
  total: string;
  subtotal: string;
  shipping: string;
  price: string;
  unit_price: string;
}

export interface SocialMediaIcons {
  name: string;
  icon: string;
  hover: string;
}
